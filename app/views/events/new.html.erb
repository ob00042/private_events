<h1>New event</h1>

<%= form_for @event do |f| %>
	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.label :date %>
	<%= f.date_select(:date, :order => [:month, :day, :year]) %>

	<%= f.submit %>
<% end %>